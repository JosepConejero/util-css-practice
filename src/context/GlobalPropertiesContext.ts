import { createContext } from "react";

export const GlobalPropertiesContext: any = createContext({});

//export const PropertiesContext: ValuesOfDOMValueAndsetValue = createContext({}); //da error de TS y no sé por qué
